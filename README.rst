===============================
HDDTEST (!! *DESCTRUCTIVE* !!)
===============================

`hddtest` is a simple utility for checking harddrives. It writes a random pice of data all over the disk and verifies the integrity of each sector afterwards. Run `make all` to build an executable.
